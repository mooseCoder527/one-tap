## Run


```bash
./gradlew desktop:run
```

Windows:

```bat
gradlew.bat desktop:run
```

Create packaged desktop distribution:

```bash
./gradlew desktop:packageDesktop
```

The packaged app will be under `build/packaged/one-tab-desktop/`.
